(() => {
  // Legacy placeholder file. The active app logic lives inline in index.php.
  // Keep this file valid JS to avoid browser parse errors if it gets requested.
  window.__waExportViewerAppJsLoaded = true;
})();
